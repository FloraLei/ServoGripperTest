# 电动夹爪控制系统 - 硬件架构与接线指南

## 📋 目录
1. [系统概述](#系统概述)
2. [硬件组件](#硬件组件)
3. [引脚分配](#引脚分配)
4. [接线图](#接线图)
5. [详细接线](#详细接线)
6. [电源分配](#电源分配)
7. [软件架构](#软件架构)
8. [数据流](#数据流)

---

## 系统概述

一个完整的电动夹爪控制系统，集成以下功能：
- **舵机控制** - Wheeltech HW020 270° 舵机
- **电流监测** - INA219 I2C 电流传感器
- **用户输入** - 3档位手动开关
- **触觉反馈** - 电流跳变时触发振动
- **数据记录** - 完整的角度和电流历史

### 系统框图

```
┌─────────────────────────────────────────────────────────────┐
│                      Arduino UNO/Nano                       │
│                                                              │
│  ┌────────────┐  ┌──────────┐  ┌─────────────────┐        │
│  │  舵机      │  │ INA219   │  │ 开关输入        │        │
│  │ 控制器     │  │ 监测     │  │ (3档位)         │        │
│  │  (PIN 8)   │  │(I2C)     │  │ (PIN 2/3/4)     │        │
│  └────────────┘  └──────────┘  └─────────────────┘        │
│         │              │              │                     │
│  ┌──────v──────────────v──────────────v──────┐             │
│  │      主控制逻辑与状态机                     │             │
│  │      - 角度计算                           │             │
│  │      - 速度控制                           │             │
│  │      - 电流跳变检测                       │             │
│  │      - 数据记录系统                       │             │
│  └──────┬──────────────┬──────────────────────┘             │
│         │              │                                     │
│  ┌──────v──────┐  ┌────v─────────┐                          │
│  │振动        │  │EEPROM配置    │                          │
│  │马达输出    │  │存储          │                          │
│  │  (PIN 6)    │  │              │                          │
│  └─────────────┘  └──────────────┘                          │
└─────────────────────────────────────────────────────────────┘
```

---

## 硬件组件

| 组件 | 型号 | 功能 | 数量 |
|------|------|------|------|
| 微控制器 | Arduino UNO/Nano | 主控制 | 1 |
| 舵机 | Wheeltech HW020 270° | 夹爪驱动 | 1 |
| 电流传感器 | INA219 | 电流监测 | 1 |
| 3档位开关 | 手动开关 | 用户控制 | 1 |
| 振动马达 | 小型马达/蜂鸣器 | 触觉反馈 | 1 |
| 电源 | 5V 3A | 舵机电源 | 1 |
| USB线 | USB-A 转 USB-B | Arduino供电 | 1 |

---

## 引脚分配

### Arduino 引脚配置

| 引脚 | 功能 | 方向 | 电压 | 说明 |
|------|------|------|------|------|
| 8 | 舵机信号 | 输出 | 5V PWM | 1000-2600µs 脉冲 |
| 6 | 振动马达 | 输出 | 5V PWM | 可选PWM调节强度 |
| 4 | 开关保持 | 输入 | 5V | 低电平有效（接地） |
| 3 | 开关关闭 | 输入 | 5V | 低电平有效（接地） |
| 2 | 开关打开 | 输入 | 5V | 低电平有效（接地） |
| A5 (SCL) | I2C时钟 | 双向 | 5V | INA219 通信 |
| A4 (SDA) | I2C数据 | 双向 | 5V | INA219 通信 |
| GND | 地线 | 参考 | 0V | **必须接到电源GND** |
| 5V | 5V输出 | 输出 | 5V | **不能给舵机供电** |

---

## 接线图

### 整体系统布局

```
┌─────────────────────────────────────────────────────────────┐
│                   外部 5V 3A 电源                           │
│                  (+5V) ─── (GND)                           │
└──────────────┬──────────────────────────┬───────────────────┘
               │                          │
               │                          │
        ┌──────v─────────┐        ┌──────v──────────┐
        │ 舵机           │        │ INA219 等       │
        │ (红: 5V+)      │        │ (多个GND)       │
        │ (黑: GND)      │        └─────────────────┘
        │ (黄: 信号)     │
        └──────┬─────────┘
               │ 仅信号
               │ 到PIN 8
        ┌──────v──────────────────────────────┐
        │         Arduino UNO/Nano            │
        │                                      │
        │  USB  ──────► 5V + GND (可选)      │
        │                                      │
        │  PIN 8  ──────► 舵机信号            │
        │  PIN 2  ◄────── 开关打开            │
        │  PIN 3  ◄────── 开关关闭            │
        │  PIN 4  ◄────── 开关保持            │
        │  PIN 6  ──────► 振动马达            │
        │  A4/A5 ◄─────► INA219 (I2C)        │
        │  GND   ───────► 公共接地            │
        └───────────────────────────────────┘
```

---

## 详细接线

### 1. 舵机连接

```
Wheeltech HW020 舵机
├─ 红线 (5V+)     → 外部5V电源 (+)
├─ 黑线 (GND)     → 外部5V电源 (-) + Arduino GND
└─ 黄线 (信号)    → Arduino PIN 8 (PWM信号)

⚠️ 关键注意事项：
- 舵机电源不能接Arduino 5V引脚
- 必须使用外部5V 3A电源
- Arduino GND 和电源GND 必须连接在一起
```

**脉冲宽度映射：**
```
0°   = 500µs   (0% = 打开位置)
45°  = 850µs
90°  = 1200µs
135° = 1550µs  (50% = 中心位置)
180° = 1900µs
225° = 2250µs
270° = 2600µs  (100% = 关闭位置)
```

### 2. INA219 电流传感器连接 (I2C)

```
INA219 传感器模块
├─ VCC       → Arduino 5V
├─ GND       → Arduino GND (也接外部电源GND)
├─ SCL       → Arduino A5 (I2C时钟)
├─ SDA       → Arduino A4 (I2C数据)
├─ VIN+      → 舵机电源 (+5V)
└─ VIN-      → 舵机电源 GND (0V)

特性：
- 测量电压：0-26V 最大
- 测量电流：±3.2A 最大
- 默认I2C地址：0x40
- 分辨率：~0.1mA
```

**I2C地址配置：**

INA219 默认使用I2C地址 0x40。如需多个传感器，可通过连接A0/A1引脚更改地址：

| A1 | A0 | 地址 |
|----|----|----|
| GND | GND | 0x40 (默认) |
| GND | Vs+ | 0x41 |
| GND | SDA | 0x42 |
| GND | SCL | 0x43 |

### 3. 三档位开关连接

```
三档位开关（如波段开关）

档位1 (打开):
└─ 开关端子1 → Arduino PIN 2
   当开关在档位1时，PIN 2 变为低电平

档位2 (关闭):
└─ 开关端子2 → Arduino PIN 3
   当开关在档位2时，PIN 3 变为低电平

档位3 (保持):
└─ 开关端子3 → Arduino PIN 4
   当开关在档位3时，PIN 4 变为低电平

公共端:
└─ 开关公共端 → Arduino GND

每个开关的电路：
Arduino PIN (2/3/4) ──[上拉10kΩ电阻]──► 5V
                     └─ [开关] ──► GND

注意：Arduino的INPUT_PULLUP内部上拉已启用，
      外部电阻是可选的但建议添加
```

### 4. 振动马达连接

```
振动马达/蜂鸣器
├─ 正极 (+5V) → Arduino PIN 6 (通过限流电阻)
└─ 负极 (GND) → Arduino GND

对于小型振动马达 (< 100mA):
- 串联220Ω电阻
- 直接连接到PIN 6

对于蜂鸣器 (> 100mA):
- 使用外部NPN三极管 (2N2222或类似)
- Arduino PIN 6 → 三极管基极 (通过1kΩ电阻)
- 三极管集电极 → 蜂鸣器正极
- 三极管发射极 → GND
- 蜂鸣器负极 → 地线

可选：在马达两端加1µF电容用于消抖
```

---

## 电源分配

### 推荐的电源方案

```
┌─────────────────────────────────────┐
│   外部 5V 3A 电源                   │
│   (USB适配器或电池包)              │
│        +5V ──┬──── GND             │
└──────────────┼────────────┬────────┘
               │            │
        ┌──────v──┐   ┌─────v──────┐
        │  舵机  │   │ INA219及   │
        │  马达  │   │ 振动马达   │
        └──────┬──┘   └─────┬──────┘
               │            │
        ┌──────v────────────v──────┐
        │  Arduino GND (公共接地)   │
        └───────────────────────────┘
        
        Arduino USB电源：
        ├─ 可选的5V用于逻辑电路
        ├─ 不能给舵机供电
        └─ 可为Arduino+传感器供电
```

### 功率预算

| 组件 | 电压 | 工作电流 | 峰值 | 说明 |
|------|------|--------|------|------|
| Arduino | 5V | ~50mA | 100mA | 逻辑+传感器 |
| INA219 | 5V | ~0.5mA | 1mA | 功率很低 |
| 舵机(空载) | 5V | ~50mA | 100mA | 保持位置 |
| 舵机(运动) | 5V | 300-800mA | 1A | 承受负载 |
| 振动马达 | 5V | 50mA | 100mA | 短脉冲 |
| **总计** | | | **3A** | 需要外部电源 |

---

## 软件架构

### 模块结构

```
gripper_main.ino
│
├─ 硬件初始化
│  ├─ 串口 (9600波特率)
│  ├─ GPIO引脚设置
│  └─ I2C + INA219设置
│
├─ 主控制循环 (20ms周期)
│  ├─ 舵机位置更新
│  ├─ 电流监测
│  ├─ 开关输入轮询
│  ├─ 振动控制
│  └─ 命令解析
│
├─ 舵机控制模块
│  ├─ PWM脉冲生成
│  ├─ 平滑位置更新
│  └─ 角度到脉冲映射
│
├─ 电流监测模块
│  ├─ INA219 I2C读取
│  ├─ 电流跳变检测
│  └─ 振动触发
│
├─ 开关输入模块
│  ├─ 引脚轮询
│  ├─ 消抖处理
│  └─ 动作执行
│
├─ 配置模块
│  ├─ EEPROM存储
│  ├─ 参数加载
│  └─ 参数保存
│
└─ 数据记录与串口模块
   ├─ 数据记录(角度+电流)
   ├─ 串口命令解析
   ├─ 状态显示
   └─ 菜单系统
```

### EEPROM布局

| 地址 | 大小 | 参数 | 默认值 | 范围 |
|------|------|------|--------|------|
| 0-1 | 2字节 | angleOpen | 0 | 0-270 |
| 2-3 | 2字节 | angleClose | 270 | 0-270 |
| 4-5 | 2字节 | moveSpeed | 5 | 1-10 |
| 6-7 | 2字节 | currentThreshold | 300 | 50-1000 |
| 8-9 | 2字节 | vibrateTime | 200 | 50-1000 |

---

## 数据流

### 正常操作序列

```
1. 初始化 (setup)
   ├─ 从EEPROM加载配置
   ├─ 初始化舵机到135°
   ├─ 初始化INA219传感器
   └─ 打印菜单到串口

2. 主循环 (每20ms)
   │
   ├─ [每100ms] 舵机更新
   │  ├─ 计算下一个角度
   │  ├─ 发送PWM脉冲
   │  └─ 更新currentAngle变量
   │
   ├─ [每100ms] 电流读取
   │  ├─ 从INA219读取电压和电流
   │  ├─ 计算与上一次的差值
   │  ├─ 检查差值是否超过阈值
   │  └─ 如果跳变超过阈值则触发振动
   │
   ├─ [持续] 开关轮询
   │  ├─ 检查PIN 2/3/4状态
   │  ├─ 消抖(等待稳定状态)
   │  └─ 执行动作(打开/关闭/停止)
   │
   ├─ [持续] 振动控制
   │  ├─ 检查振动超时是否到达
   │  └─ 如果时间已过则关闭马达
   │
   └─ [持续] 串口命令解析
      ├─ 检查是否有传入数据
      ├─ 解析命令字符串
      └─ 执行相应动作

3. 用户操作
   │
   ├─ 手动开关输入
   │  ├─ 读取开关位置
   │  ├─ 立即移动夹爪
   │  └─ 用时间戳和电流记录动作
   │
   └─ 串口命令输入
      ├─ 用户输入命令(如"open")
      ├─ 命令被解析和验证
      ├─ 动作被执行
      └─ 响应发送到串口
```

### 电流跳变检测逻辑

```
当INA219读取电流时：

1. 从INA219读取电流
   └─ currentCurrent (mA)

2. 计算差值
   └─ delta = |currentCurrent - lastCurrent|

3. 检查阈值
   ├─ 如果 delta >= currentThreshold
   │  ├─ 记录"spike_detected"
   │  ├─ 启动振动马达
   │  ├─ 打印警告到串口
   │  └─ 振动vibrateTime毫秒
   │
   └─ 否则
      └─ 继续正常运行

4. 更新上一次读取
   └─ lastCurrent = currentCurrent
```

---

## 工作模式

### 模式1：手动开关控制
```
用户位置 → 开关检测 → Arduino读取PIN → 执行动作
                    (消抖处理)

- 不需要串口输入
- 响应迅速(~50ms包括消抖)
- 自动记录带时间戳和电流的日志
```

### 模式2：串口命令控制
```
串口输入 → 解析命令 → 验证 → 执行 → 记录+响应
(通过USB/串口监视器)

例如: "open" → 移动到angleOpen → 记录动作 → 打印"✓"
```

### 模式3：自主电流检测控制
```
舵机运动 → INA219监测 → 检测到电流跳变 → 振动
                    (每100ms)

- 在后台自动运行
- 不需要用户输入
- 用于检测夹爪是否"抓住"物体
```

---

## 接线故障排除

| 症状 | 可能原因 | 解决方案 |
|------|--------|--------|
| 舵机不动 | 错误的PIN或没有电源 | 检查PIN 8和5V 3A电源 |
| INA219检测不到 | I2C连接问题 | 检查SDA/SCL (A4/A5)连接 |
| 开关不工作 | 错误的PIN或线路松动 | 验证PIN 2/3/4和GND连接 |
| 振动不工作 | PIN 6问题或马达故障 | 直接测试马达或改变PIN |
| 舵机抖动 | 电源不稳定 | 使用独立5V电源，添加电容 |
| 电流读取错误 | INA219线路松动 | 重新连接I2C引脚 |
| 无法上传代码 | USB问题 | 尝试不同的线或USB接口 |

---

## 组件规格

### Wheeltech HW020 舵机
```
控制信号：   标准PWM (50Hz = 20ms周期)
脉冲宽度：   500-2600µs (0-270°)
工作电压：   5-7.4V
失速电流：   5V时 500-800mA
重量：       ~55g
尺寸：       40.7 x 20.1 x 38.2mm
扭矩：       5V时 10kg·cm
```

### INA219 电流传感器
```
工作电压：   3.3-5V
最大测量电压：26V
最大电流：   ±3.2A
I2C地址：   0x40 (默认)
分辨率：    ~0.1mA (10位ADC)
响应时间：   ~1ms
尺寸：      ~15 x 13mm
```

### Arduino UNO/Nano
```
微控制器：   ATmega328P
工作电压：   5V
时钟速度：   16MHz
数字引脚：   14 (其中6个支持PWM)
模拟引脚：   6 (A0-A5)
EEPROM：    1024字节
SRAM：      2048字节
Flash：     32KB (可用代码28KB)
```

---

## 安全考虑

⚠️ **电源供电安全**
- 必须使用**稳压5V 3A电源**
- 使用适当的电源连接器(避免松散连接)
- 在电源线上添加主开关

⚠️ **机械安全**
- 夹爪可以施加力量 - 远离手指
- 先用空夹爪测试
- 监测电流以检测异常负载

⚠️ **电气安全**
- 确保整个系统接地正确
- 使用适当的导线规格(电源至少22AWG)
- 远离水和液体

⚠️ **软件安全**
- 不要将打开和关闭角度设为相同值
- 监测电流跳变阈值设置
- 保留串口日志以查看操作历史

---

## 测试检查清单

- [ ] Arduino成功上传代码
- [ ] 串口监视器显示"✓ INA219 initialized"
- [ ] 舵机响应"open"命令(转到0°)
- [ ] 舵机响应"close"命令(转到270°)
- [ ] 电流读数合理(50-800mA)
- [ ] 手动开关档位1打开夹爪
- [ ] 手动开关档位2关闭夹爪
- [ ] 手动开关档位3停止夹爪
- [ ] 命令时振动马达启动
- [ ] 数据日志显示带时间戳和电流的条目
- [ ] 配置保存到EEPROM

---

## 进一步参考

- [Adafruit INA219 指南](https://learn.adafruit.com/adafruit-ina219-current-sensor-breakout)
- [Arduino PWM参考](https://www.arduino.cc/reference/en/language/functions/analog-io/analogwrite/)
- [Arduino I2C参考](https://www.arduino.cc/en/Reference/Wire)
- [舵机马达控制](https://www.arduino.cc/reference/en/libraries/servo/)

