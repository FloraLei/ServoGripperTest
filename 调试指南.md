# 电动夹爪控制系统 - 调试指南

## 目录
1. [硬件连接验证](#硬件连接验证)
2. [逐模块测试](#逐模块测试)
3. [传感器校准](#传感器校准)
4. [常见问题和解决方案](#常见问题和解决方案)
5. [性能测试](#性能测试)
6. [日志分析](#日志分析)
7. [紧急故障排除](#紧急故障排除)

---

## 硬件连接验证

### 初始上电检查
1. **上电前：**
   - 使用 HARDWARE_ARCHITECTURE.md / 硬件架构与接线指南.md 验证所有连接
   - 检查是否有松动的线路或虚焊
   - 确保舵机电源与 Arduino 分开（不使用 USB 供电）
   - 确认 Arduino 和外部电源共享 GND

2. **上电时：**
   - 听舵机初始化时的声音（轻微运动或嗡鸣）
   - 检查 INA219 LED 指示灯是否亮起（如果有）
   - 验证串口监视器在 9600 波特率下能打开
   - 查看是否输出横幅消息

### 快速连接测试
```
预期串口输出：
==========================================
  Electric Gripper Control System v2.0
==========================================
✓ Servo: PIN 8 (Wheeltech HW020 270°)
✓ INA219: I2C (A4/A5)
✓ Switch: PIN 2/3/4 (3-position)
✓ Vibration: PIN 6
✓ Power: External 5V 3A
==========================================

✓ INA219 initialized
```

如果看到 `✗ INA219 not found!`，请参考 [INA219 无反应](#ina219-无反应)。

---

## 逐模块测试

### 1. 舵机马达控制

#### 测试 1：基本运动
```
串口命令：  go 0
预期结果：  舵机移动到打开位置，输出：
           → Moving to 0° | Current: X mA

串口命令：  go 270
预期结果：  舵机平滑移动到关闭位置
```

**如果舵机不动：**
- 检查 PIN 8 是否连接到舵机信号线（白色线）
- 验证舵机电源（红色线）是否为 5V 且电流充足
- 检查 GND 连接（黑色线）
- 尝试：`go 135`（中间位置）

**如果舵机运动缓慢：**
- 检查舵机电源电压（应该稳定在 5V）
- 减少其他模块的电流消耗
- 正常速度取决于 `moveSpeed` 设置（默认 5°/100ms）

**如果舵机抖动或卡顿：**
- 在舵机电源上并联 100µF 电容
- 确保舵机电源有充足的电流容量（最少 3A）
- 检查是否受到振动马达的 EMI 干扰 - 如果可能，分离电源线

#### 测试 2：角度精度
```
串口命令：  go 0, go 45, go 90, go 135, go 180, go 225, go 270
检查项：   舵机到达每个位置时平滑，无超调
测量：     用量角器手动检查角度（±5° 可接受）
```

如果角度偏差 >10°：
- 用 `simple_pulse_test.ino` 进行舵机校准
- 检查舵机是否有机械间隙或齿轮磨损
- 验证舵机在初始运动前是否处于中立位置

#### 测试 3：速度调节
```
串口命令：  set speed 1
然后：      go 270
预期：      缓慢运动（~1°/100ms）

串口命令：  set speed 10
然后：      go 0
预期：      快速运动（~10°/100ms）
```

如果速度不变，验证设置是否已保存：
```
串口命令：  config
检查：      "Movement speed: X°/100ms"
```

---

### 2. INA219 电流传感器

#### 测试 1：基本电流读数
```
串口命令：  current
预期输出：  Current: 50-100 mA（无负载时静止）
```

**如果显示 "Current: 0 mA"：**
- 检查 I2C 连接（SCL 在 A5，SDA 在 A4）
- 验证 INA219 模块是否有 5V 电源
- 检查 GND 连接是否松动
- 检查 INA219 板上是否有烧焦的元件

**如果值波动很大（跳变）：**
- 在 INA219 电源上添加 0.1µF 陶瓷电容
- 确保电源在负载下稳定
- 检查是否有松动的连接导致接触不良

#### 测试 2：运动时的电流
```
串口命令：  config            （记录当前阈值）
串口命令：  go 270             （关闭夹爪）
串口命令：  log
检查项：   电流在舵机遇到阻力时增加
预期：     从 ~80mA 逐渐增加到 200-400mA（带负载）
```

**如果电流超过阈值：**
```
检查日志：  "⚡ Current spike detected! Delta: XXX mA"
验证：      振动马达启动（应该能感觉到振动）
```

#### 测试 3：电流阈值调节
```
串口命令：  set threshold 250  （降低阈值）
串口命令：  go 270
预期：      在关闭时更早触发脉冲

串口命令：  set threshold 500  （提高阈值）
串口命令：  go 0
串口命令：  go 270
预期：      只有在重负载时才触发脉冲
```

---

### 3. 三档位开关

#### 测试 1：打开位置（PIN 2）
```
操作：      按下并释放打开开关
预期输出：  🔓 Switch: Open
           🔓 Opening gripper...
           → Moving to 0° | Current: X mA
```

**如果没有任何反应：**
- 检查 PIN 2 是否连接到开关位置 1
- 用万用表验证开关是否能导通
- 确认开关有上拉电阻或使用了 INPUT_PULLUP
- 检查是否存在去抖问题（长按开关）

#### 测试 2：关闭位置（PIN 3）
```
操作：      按下并释放关闭开关
预期输出：  🔒 Switch: Close
           🔒 Closing gripper...
           → Moving to 270° | Current: X mA
```

#### 测试 3：保持位置（PIN 4）
```
操作：      先打开夹爪（串口：open）
串口命令：  go 135              （手动移动到中间）
操作：      按下保持开关
预期输出：  ⏸ Switch: Hold
           ⏸ Stopping gripper
行为：      夹爪停在 135°，不再运动
```

#### 测试 4：开关去抖
```
操作：      快速轻点开关多次
预期输出：  每次按压只应识别一次
           （如果识别多次，说明去抖延迟过短）
```

---

### 4. 振动马达反馈

#### 测试 1：手动触发
```
串口命令：  test vibrate
预期输出：  📳 Vibration started
持续时间：  马达振动 200ms（默认）
           📳 Vibration stopped

感觉：      应该能感受到明显的振动/嗡鸣
```

**如果没有振动：**
- 检查 PIN 6 是否连接到振动马达
- 验证马达是否有 5V 电源
- 检查 GND 连接
- 用 5V 直接电源测试马达（绕过 Arduino 以隔离问题）

**如果振动很弱：**
- 检查电源电压（应该是 5V，不是 4.5V）
- 验证电源电流容量（振动马达可能吸收 500mA+）
- 确保电源容量充足

#### 测试 2：电流峰值触发
```
串口命令：  set threshold 150   （降低阈值用于测试）
串口命令：  go 270              （关闭夹爪）
手动应用：  对夹爪施加外部压力
预期输出：  ⚡ Current spike detected!
           📳 Vibration started
           [马达振动]
           📳 Vibration stopped
```

#### 测试 3：振动时长配置
```
串口命令：  set vibrate 50      （很短）
串口命令：  test vibrate
检查项：   振动约 50ms

串口命令：  set vibrate 500     （较长）
串口命令：  test vibrate
检查项：   振动约 500ms
```

---

## 传感器校准

### 舵机校准（如果角度不准确）

1. **上传 `simple_pulse_test.ino`：**
   - 测试脉冲宽度：500, 1050, 1550, 2100, 2600 µs
   - 每个位置保持 5 秒
   - 用量角器测量实际角度

2. **如果测量的角度与预期不同：**
   - 记录脉冲值和实际角度
   - 更新 `gripper_main.ino` 中的 `getPulseForAngle()` 查表
   - 用 `go` 命令重新测试

3. **精密搜索，上传 `fine_search.ino`：**
   - 在 1400-1600 µs 范围内以 10 µs 步长测试
   - 找到舵机的精确脉冲边界
   - 更新 PULSE_MIN 和 PULSE_MAX 常数

### INA219 校准

**检查内部阻抗：**
```
1. 断开负载（夹爪不夹持）
2. 串口命令：current
3. 记录基线电流（无运动时应为 0-50 mA）

4. 如果基线 >100 mA：
   - 检查接线中是否有短路
   - 验证舵机是否被压在机械止点
   - 测试舵机马达隔离
```

**检查测量精度：**
```
1. 用万用表测量负载两端的实际电压
2. 与 INA219 读数比较
3. 如果差异 >10%，调整 INA219 配置（见数据手册）
```

---

## 常见问题和解决方案

### 问题 1：舵机只转 3-5°
**症状：** 舵机运动但范围极其有限
**根本原因：** 角度计算中的整数除法精度丢失
**解决方案：** 当前版本已修复 - 使用查表法而不是公式
**验证：** 检查使用的是 `getPulseForAngle()`，而不是旧公式

### 问题 2：INA219 无反应
**症状：** 串口输出显示 `✗ INA219 not found!`
**故障排除：**
```
1. 检查 I2C 地址：
   - INA219 默认地址：0x40
   - 如果不同，修改：ina219.begin(0x4F) 为正确地址

2. 测试 I2C 连接：
   上传 i2c_scanner 草图（标准 Arduino 示例）
   在结果中查看地址 0x40

3. 检查物理连接：
   - SCL: A5（白色线）
   - SDA: A4（绿色线）
   - GND: GND（黑色线）
   - 5V:  5V（红色线）

4. 尝试不同的 I2C 引脚：
   如果使用 Arduino Mega：SCL=21, SDA=20（修改 Wire.begin()）

5. 验证 Adafruit 库：
   - 检查版本：草图 → 包含库 → 管理库
   - 如果超过 6 个月未更新，请更新
```

### 问题 3：舵机抖动或颤抖
**症状：** 舵机在保持位置时抖动
**原因和解决方案：**
```
1. 电源不足：
   - 升级到 5V 3A 或更高
   - 在舵机电源上添加 470µF + 0.1µF 电容

2. I2C 线路噪声：
   - 使用屏蔽线作为 I2C
   - 将 I2C 线保持远离舵机电源线
   - 如果没有，添加 4.7kΩ 上拉电阻

3. USB 噪声干扰：
   - 从外部 5V 电源给 Arduino 供电，不使用 USB
   - 如果必须保持 USB 连接，在 USB 线上添加铁氧体磁环

4. PWM 频率冲突：
   - 避免对其他模块使用 PIN 9 的 PWM
   - 将振动马达电源分开
```

### 问题 4：开关不响应
**症状：** 按下开关没有任何反应
**故障排除：**
```
1. 测试物理开关：
   - 将万用表设为连续性模式
   - 按每个位置，应该听到/看到接触

2. 测试 Arduino 引脚：
   - 上传 BasicDigitalRead 草图
   - 按下时监视 PIN 2/3/4 值
   - 按下时应为 LOW (0)

3. 检查去抖：
   - 如果过于敏感，增加 checkSwitches() 中的 delay(50)
   - 验证上拉电阻是否存在

4. 重置 EEPROM：
   - 上传 gripper_main.ino
   - 串口命令：set open 0
   - 串口命令：set close 270
   - 这会强制 EEPROM 重置
```

### 问题 5：电流读数显示 0 或负值
**症状：** `current` 命令返回 0 或负数值
**故障排除：**
```
1. 检查 INA219 电源：
   - 万用表测 INA219 的 5V 和 GND 引脚
   - 应显示稳定的 5V

2. 检查分流电阻连接：
   - INA219 通过微小分流电阻测量电压
   - 确保舵机电源通过分流端子
   - 不绕过传感器

3. 验证电流方向：
   - 正常工作时，电流从 IN+ 流向 IN-
   - 反向连接读数为负

4. 检查短路：
   - 断开舵机
   - 用被动负载（电阻）测试电流
```

---

## 性能测试

### 负载测试
```
设置：      用已知重量的物体夹住
命令：      set threshold 250
           go 270
           log
```

**检查结果：**
- 电流在夹爪关闭时平滑增加
- 物体被完全夹住时触发峰值
- 马达振动以信号物体被检测到
- 无抖动或振荡

### 速度测试
```
测量完整范围所用时间（0° → 270°）：
命令：      go 270
时间：      以速度 5 应约为 5.4 秒
           （270° ÷ 5°/100ms = 5400ms）

修改速度：  set speed 10
重新测试：  应约为 2.7 秒
```

### 耐久性测试
```
重复 100 次：
- 打开夹爪（go 0）
- 关闭夹爪（go 270）
- 测量最终状态

检查项：
- 舵机是否有磨损或性能下降
- 内存泄漏（RAM 使用应稳定）
- EEPROM 损坏（设置不变）
- 马达发热（用手触摸舵机检查温度）
```

---

## 日志分析

### 查看日志
```
串口命令：  log
输出格式：  Time(s) | Angle | Current(mA) | Action
例如：
Time(s) | Angle | Current(mA) | Action
--------|-------|-------------|------------------
5       | 0°    | 85          | open
12      | 270°  | 410         | spike_detected
18      | 270°  | 95          | close
```

### 查看内容

**1. 电流变化模式：**
```
✓ 正常：    85 → 120 → 150 → 200 → 350（平滑增加，然后触发）
✗ 问题：    400 → 300 → 500 → 100（不规则，缺少峰值阈值）
```

**2. 峰值检测时间：**
```
✓ 正确：    夹爪遇到阻力时在 300mA 检测到电流峰值
✗ 问题：    无负载时检测峰值 / 满负载时不检测
解决方案：  用命令调整阈值：set threshold XXX
```

**3. 操作序列：**
```
✓ 合理：    open → move → spike_detected → stop
✗ 错误：    spike_detected（无负载）→ move → stop
```

### 清除旧日志
```
串口命令：  clear log
输出：      ✓ Logs cleared
验证：      log
           （应显示空日志）
```

---

## 紧急故障排除

### 舵机无反应（什么都不工作）

**步骤 1：验证电源**
```
万用表测舵机电源（红/黑）：
- 应显示稳定的 5V
- 不是 4V 或波动的
```

**步骤 2：直接测试舵机**
```
上传 simple_pulse_test.ino 到 Arduino
- 如果舵机动：控制器问题
- 如果舵机不动：舵机或电源问题
```

**步骤 3：检查信号线**
```
万用表测 PIN 8（信号线）：
- 应该看到脉冲信号（变化的 0-5V 电压）
- 如果有示波器，更能看清
```

### 多个模块不工作

**完整重置过程：**
```
1. 断开所有电源
2. 等待 30 秒
3. 从 PIN 8 断开舵机信号线
4. 重新连接 Arduino 电源（USB）
5. 打开串口监视器
6. 验证横幅出现
7. 重新连接舵机信号
8. 测试：go 90
```

### 代码更新后出现异常行为

**工厂重置：**
```
1. 上传任何草图（甚至空白）
2. 断开 Arduino 电源
3. 断开舵机外部电源
4. 等待 1 分钟
5. 重新连接电源
6. 上传 gripper_main.ino
7. 重置 EEPROM：set open 0, set close 270
```

---

## 调试用串口命令参考

| 命令 | 目的 | 输出例 |
|------|------|--------|
| `pos` | 显示当前角度 | `Angle: 135° \| Current: 95 mA` |
| `current` | 显示电流读数 | `Current: 120 mA` |
| `config` | 显示所有设置 | 显示所有参数 |
| `log` | 显示操作历史 | 所有操作的表格 |
| `clear log` | 清除日志历史 | `✓ Logs cleared` |
| `test vibrate` | 测试振动马达 | 按配置时间振动 |
| `go <angle>` | 移动到角度 | `→ Moving to 180° \| Current: 95 mA` |
| `open` | 移动到打开位置 | `🔓 Opening gripper...` |
| `close` | 移动到关闭位置 | `🔒 Closing gripper...` |
| `set threshold <mA>` | 设置峰值阈值 | `✓ Current threshold set to 250 mA` |
| `set speed <1-10>` | 设置运动速度 | `✓ Speed set to 3°/100ms` |
| `help` | 显示所有命令 | 完整命令菜单 |

---

## 何时联系支持

如果完成所有调试步骤后仍有问题：
1. 运行完整日志并添加时间戳
2. 记录精确症状和复现步骤
3. 检查 Arduino IDE 版本（推荐 1.8.19+）
4. 验证所有硬件连接与硬件架构文档匹配
5. 确认 Adafruit_INA219 库是最新版本
