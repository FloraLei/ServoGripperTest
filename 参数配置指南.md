# 电动夹爪控制系统 - 参数配置指南

## 简介

电动夹爪系统有 5 个主要可配置参数，通过串口命令可以实时修改，并自动保存到 EEPROM 非易失性存储。本指南详细说明每个参数的含义、默认值、调整范围及其影响。

---

## 参数概览表

| 参数名 | 命令 | 默认值 | 范围 | 单位 | 说明 |
|--------|------|--------|------|------|------|
| 打开角度 | `set open` | 0 | 0-270 | 度(°) | 夹爪打开位置 |
| 关闭角度 | `set close` | 270 | 0-270 | 度(°) | 夹爪关闭位置 |
| 运动速度 | `set speed` | 5 | 1-10 | °/100ms | 舵机转动速度 |
| 电流阈值 | `set threshold` | 300 | 50-1000 | mA | 触发振动的电流跳变 |
| 振动时长 | `set vibrate` | 200 | 50-1000 | ms | 触发时振动持续时间 |

---

## 1. 打开角度（Open Angle）

### 命令格式
```
set open <0-270>
```

### 说明
定义夹爪的"打开"位置。按下打开开关或执行 `open` 命令时，舵机将转到该角度。

### 默认值
```
0° (完全打开)
```

### 调整范围
- **最小：** 0°（舵机物理极限）
- **最大：** 270°（舵机物理极限）
- **推荐：** 0-50° 之间

### 实际应用

**场景 1：轻物体（餐具、笔）**
```
推荐：set open 0
说明：完全打开，便于放取轻物体
```

**场景 2：中等物体（手柄）**
```
推荐：set open 10-20
说明：略微张开，防止物体滑落
```

**场景 3：对称夹爪**
```
推荐：set open 45
说明：对称张开，美观且功能均衡
```

### 设置方法

**步骤 1：初始化测试**
```
1. 上传 gripper_main.ino 到 Arduino
2. 打开串口监视器（9600 波特率）
3. 执行：open
4. 观察舵机运动到的位置
5. 执行：pos （查看当前角度）
```

**步骤 2：手动调整**
```
1. 执行：set open 20
2. 执行：open
3. 观察夹爪打开的位置
4. 如果需要更大张开，尝试 set open 30
5. 如果需要更小张开，尝试 set open 10
6. 找到满意的位置后，会自动保存
```

**步骤 3：验证**
```
执行：config
查看：Open angle: 20°（确认已保存）
```

### 对系统的影响

| 设置值 | 影响 |
|--------|------|
| 0° | 最大张开，适合轻物体 |
| 10-20° | 轻度张开，平衡开启空间和夹持力 |
| 30-50° | 中等张开，适合中等大小物体 |
| >50° | 建议避免，夹爪打开角度过小 |

---

## 2. 关闭角度（Close Angle）

### 命令格式
```
set close <0-270>
```

### 说明
定义夹爪的"关闭"位置。按下关闭开关或执行 `close` 命令时，舵机将转到该角度。

### 默认值
```
270° (完全关闭)
```

### 调整范围
- **最小：** 0°（舵机物理极限）
- **最大：** 270°（舵机物理极限）
- **推荐：** 220-270° 之间

### 实际应用

**场景 1：最大夹持力（重物）**
```
推荐：set close 270
说明：舵机完全旋转，获得最大力矩和夹持力
```

**场景 2：轻度夹持**
```
推荐：set close 180-200
说明：减少对脆弱物体的损伤，如鸡蛋、塑料杯
```

**场景 3：恰好接触（仅触碰）**
```
推荐：set close 150
说明：仅与物体接触但不施加压力，用于感应
```

### 设置方法

**步骤 1：确定目标**
```
问题：需要多大的夹持力？
- 重物体？ → 设置为 270°
- 轻物体？ → 设置为 200-240°
- 仅感应？ → 设置为 150-180°
```

**步骤 2：逐步调整**
```
1. 执行：set close 270（从最大开始）
2. 执行：close
3. 观察夹爪闭合位置和力度
4. 执行：set close 250（逐步减小）
5. 执行：close（重新测试）
6. 重复直到找到理想位置
```

**步骤 3：联合测试**
```
执行：set threshold 200   （降低电流阈值以便测试）
执行：close
观察：电流数值和震动反馈
调整：如果电流过高，可适当提高 close 角度
```

### 对系统的影响

| 设置值 | 影响 | 典型应用 |
|--------|------|----------|
| 270° | 最大夹持力，完全关闭 | 重物体、金属零件 |
| 240-260° | 强夹持力 | 中等物体、纸张 |
| 200-230° | 中等夹持力 | 轻物体、塑料 |
| 150-180° | 轻度夹持 | 易碎物体、传感器 |
| <150° | 仅接触，不施压 | 纯触觉感应 |

---

## 3. 运动速度（Movement Speed）

### 命令格式
```
set speed <1-10>
```

### 说明
控制舵机从当前位置转到目标位置的速度。数值代表每 100 毫秒转过的角度数。

### 默认值
```
5 (5°/100ms = 50°/秒)
```

### 调整范围

| 设置值 | 转速 | 特点 |
|--------|------|------|
| 1 | 1°/100ms (10°/秒) | 超慢，精细控制 |
| 2 | 2°/100ms (20°/秒) | 很慢，稳定平缓 |
| 3 | 3°/100ms (30°/秒) | 慢速，精准 |
| 4 | 4°/100ms (40°/秒) | 偏慢，可靠 |
| **5** | **5°/100ms (50°/秒)** | **默认，均衡** |
| 6 | 6°/100ms (60°/秒) | 偏快，响应灵敏 |
| 7 | 7°/100ms (70°/秒) | 快速，动态感强 |
| 8 | 8°/100ms (80°/秒) | 很快，需要稳定电源 |
| 9 | 9°/100ms (90°/秒) | 超快，可能导致抖动 |
| 10 | 10°/100ms (100°/秒) | 最快，高功耗 |

### 速度计算公式

从 0° 到 270° 完整转动所需时间：
```
时间(秒) = 270° ÷ 速度(°/100ms) × 0.1秒

例：速度=5时，时间 = 270 ÷ 5 × 0.1 = 5.4 秒
例：速度=10时，时间 = 270 ÷ 10 × 0.1 = 2.7 秒
```

### 实际应用

**场景 1：精细装配（机械臂）**
```
推荐：set speed 2-3
优点：精确定位，易于控制
缺点：速度慢，容易掉落轻物
```

**场景 2：日常使用（手持工具）**
```
推荐：set speed 5-6（默认）
优点：速度和精度的平衡
缺点：无特别缺点，适合大多数场景
```

**场景 3：快速操作（流水线）**
```
推荐：set speed 8-10
优点：反应迅速，效率高
缺点：容易抖动，需要稳定电源
```

**场景 4：轻物体夹取**
```
推荐：set speed 3-4
优点：减少物体受到的冲击力
缺点：相对较慢
```

### 电源影响

速度越高，瞬时功耗越大。不同速度下的功耗关系：

```
功耗 ≈ (舵机扭矩 × 角速度) + 基础功耗

速度=5°/100ms: 约 1-2A（推荐最小电源）
速度=10°/100ms: 约 2-3A（需要充足电源）
```

**电源建议：**
- 速度 1-5：5V 2A 电源足够
- 速度 6-8：建议 5V 2.5A 以上
- 速度 9-10：建议 5V 3A（刚好够用）

### 设置方法

**步骤 1：查看当前速度**
```
执行：config
查看：Movement speed: X°/100ms
```

**步骤 2：调整速度**
```
执行：set speed 3
执行：go 270          （观察速度）
等待完成后：
执行：set speed 8
执行：go 0            （对比速度）
```

**步骤 3：根据电源调整**
```
如果看到舵机抖动或不稳定：
→ 执行：set speed 5  （降低速度）

如果速度太慢，影响工作效率：
→ 确认电源充足
→ 执行：set speed 8  （提高速度）
```

### 对系统的影响

| 速度 | 功耗 | 稳定性 | 精度 | 推荐场景 |
|------|------|--------|------|----------|
| 1-2 | 低 | 极好 | 极高 | 精密装配 |
| 3-4 | 低 | 很好 | 高 | 轻物体、易碎品 |
| 5-6 | 中 | 好 | 中 | 通用场景 |
| 7-8 | 中高 | 一般 | 中 | 流水线 |
| 9-10 | 高 | 不稳定 | 低 | 仅高速应用 |

---

## 4. 电流阈值（Current Threshold）

### 命令格式
```
set threshold <50-1000>
```

### 说明
定义电流变化的灵敏度。当电流变化量超过此值时，系统认为夹爪接触到物体，触发振动反馈。

### 默认值
```
300 mA (毫安)
```

### 调整范围

| 范围 | 灵敏度 | 应用 |
|------|--------|------|
| 50-100 mA | 极高灵敏 | 传感器应用、轻触检测 |
| 100-200 mA | 高灵敏 | 轻物体检测 |
| **200-300 mA** | **中等（推荐）** | **通用应用** |
| 300-500 mA | 低灵敏 | 重物体、粗糙作业 |
| 500-1000 mA | 极低灵敏 | 需要高载荷确认 |

### 工作原理

```
系统监测：当前电流值 - 上一次电流值
当差值 >= 阈值时：触发振动反馈

例：
- 上次读数：50 mA
- 当前读数：350 mA
- 差值：300 mA
- 如果阈值设为 300 mA：触发！
- 如果阈值设为 400 mA：不触发
```

### 实际应用

**场景 1：轻物体检测（传感）**
```
推荐：set threshold 100-150
含义：任何轻微接触都会触发
应用：精细操作、易碎品保护
特点：反应灵敏，可能误触
```

**场景 2：标准应用（通用）**
```
推荐：set threshold 250-300
含义：明确接触到物体时触发
应用：日常使用、通用场景
特点：反应及时，误触极少
```

**场景 3：重物体应用**
```
推荐：set threshold 400-500
含义：需要实际承重后才触发
应用：重物搬运、工业应用
特点：反应稳定，不易误触
```

**场景 4：纯粹检测（不检测夹爪状态）**
```
推荐：set threshold 800-1000
含义：需要非常大的电流变化
应用：特殊场景，如测量最大力
特点：只在极端情况下触发
```

### 设置方法

**步骤 1：理解电流基线**
```
执行：open           （打开夹爪）
等待2秒：
执行：current        （记录无负载电流，通常 50-100 mA）
```

**步骤 2：测试新阈值**
```
执行：set threshold 200
执行：go 270         （闭合，但不夹物体）
观察：是否触发？不应该（无物体）

执行：open           （打开）
用手指轻轻放在夹爪间
执行：go 270         （闭合）
观察：是否触发？应该（有接触）
```

**步骤 3：微调以达最佳**
```
如果经常误触（无物体也触发）：
→ 执行：set threshold 350

如果难以触发（轻物体接触也不响应）：
→ 执行：set threshold 150

查看日志确认触发：
→ 执行：log
→ 查看是否有 spike_detected 条目
```

### 典型电流值参考

```
不同场景下的测量电流（仅供参考）：

空闲状态：              50-80 mA
缓慢转动：              100-150 mA
转动到中等阻力：        200-250 mA
接触轻物体（如纸）：    250-350 mA
夹住中等物体（如杯子）: 350-500 mA
夹住重物体（如金属件）: 500-800 mA
堵转/最大力：           >800 mA
```

---

## 5. 振动时长（Vibration Time）

### 命令格式
```
set vibrate <50-1000>
```

### 说明
定义当电流超过阈值时，振动马达持续工作的时间。

### 默认值
```
200 ms (毫秒 = 0.2 秒)
```

### 调整范围

| 时长 | 感觉 | 应用 |
|------|------|------|
| 50 ms | 极短脉冲 | 快速反馈，连续工作 |
| 100 ms | 短振动 | 轻微提示 |
| **200 ms** | **明显振动（默认）** | **标准反馈** |
| 300-400 ms | 长振动 | 强提示 |
| 500-1000 ms | 很长振动 | 警告级别 |

### 用户体验

```
振动时长对用户反馈的影响：

50 ms:   "嘘" 的一下，需要专注才能感受
100 ms:  轻微的提示，易被忽视
200 ms:  清晰的反馈，易察觉（推荐）
300 ms:  强烈的反馈，不易忽视
500+ ms: 持久的警告，确保被注意到
```

### 实际应用

**场景 1：精细操作（手持工具）**
```
推荐：set vibrate 150-200
说明：及时反馈，不打断操作流
特点：反应快速，用户体验好
```

**场景 2：安全应用（防护提示）**
```
推荐：set vibrate 300-500
说明：强烈反馈，确保注意到
特点：不易被忽视，安全可靠
```

**场景 3：流水线作业（高频次）**
```
推荐：set vibrate 100
说明：快速反馈，不影响节奏
特点：高效率，快速周期
```

**场景 4：轻提示（背景反馈）**
```
推荐：set vibrate 50-100
说明：仅作为轻微提示
特点：不打扰，易于忽视
```

### 功耗考虑

```
振动马达功耗：约 500mA（持续时）

不同时长下的总能量消耗：
50 ms：  50 mA·s（极低）
100 ms： 100 mA·s（低）
200 ms： 200 mA·s（中，默认）
500 ms： 500 mA·s（高）
1000 ms: 1000 mA·s（很高）
```

**建议：** 如果电源有限，保持在 200 ms 或更短。

### 设置方法

**步骤 1：测试各种时长**
```
执行：set vibrate 100
执行：test vibrate     （感受 100ms 振动）
等待2秒：
执行：set vibrate 200
执行：test vibrate     （感受 200ms 振动）
等待2秒：
执行：set vibrate 300
执行：test vibrate     （感受 300ms 振动）
```

**步骤 2：根据需要选择**
```
如果觉得 200ms 刚好：
→ 保持默认，无需改动

如果觉得反馈太强：
→ 执行：set vibrate 100

如果觉得反馈不明显：
→ 执行：set vibrate 300
```

**步骤 3：在实际使用中验证**
```
执行：set threshold 250
执行：close            （关闭夹爪）
用手指轻轻插入
执行：go 270           （闭合）
感受振动强度和时长
调整至满意为止
```

---

## 参数相互关系

### 1. 速度 vs 电流阈值

```
场景：快速关闭重物

高速 (speed=10) + 高阈值 (threshold=500)
→ 快速闭合，电流增长快
→ 可能在达到阈值前已经堵转
→ 触发可能滞后

低速 (speed=3) + 低阈值 (threshold=150)
→ 缓慢闭合，电流增长缓慢
→ 更容易及时检测到物体
→ 触发及时准确

推荐配合：
- 高速 → 高阈值（300-400）
- 低速 → 低阈值（150-200）
- 中速 → 中阈值（250-300）
```

### 2. 关闭角度 vs 电流阈值

```
场景：轻物体检测

大闭合度 (close=270) + 低阈值 (threshold=100)
→ 力度大，但电流变化明显
→ 易于触发

小闭合度 (close=150) + 高阈值 (threshold=500)
→ 力度小，但需要大电流变化才能触发
→ 可能难以触发

推荐配合：
- 大闭合力 → 中等阈值（200-300）
- 小闭合力 → 低阈值（100-150）
```

### 3. 打开/关闭角度 vs 速度

```
场景：大角度移动

大角度范围 (270°) + 低速 (speed=2)
→ 需要 13.5 秒
→ 低效但精确

大角度范围 (270°) + 高速 (speed=10)
→ 需要 2.7 秒
→ 高效但消耗功率大

推荐配合：
- 精细操作 → 小范围角度 + 低速
- 快速操作 → 大范围角度 + 高速
```

---

## 常用配置模板

### 模板 1：精细装配
```
set open 0
set close 180           （轻度夹持）
set speed 2             （超慢）
set threshold 150       （高灵敏）
set vibrate 100         （轻提示）

特点：精准、缓慢、易碎品安全
```

### 模板 2：通用（推荐）
```
set open 0
set close 270           （完全关闭）
set speed 5             （中等速度）
set threshold 300       （标准灵敏）
set vibrate 200         （明显反馈）

特点：平衡、可靠、适合大多数场景
```

### 模板 3：快速流水线
```
set open 10
set close 260           （大部分关闭）
set speed 8             （很快）
set threshold 350       （低灵敏，避免误触）
set vibrate 100         （短反馈）

特点：快速、高效、适合重复操作
```

### 模板 4：轻物体专用
```
set open 0
set close 200           （轻度夹持）
set speed 3             （缓慢）
set threshold 100       （极高灵敏）
set vibrate 300         （强反馈）

特点：温和、敏感、保护轻物体
```

### 模板 5：重物搬运
```
set open 20
set close 270           （最大力度）
set speed 7             （快速）
set threshold 500       （低灵敏，只检测重物）
set vibrate 200         （确保反馈）

特点：强力、快速、适合工业应用
```

---

## 保存和恢复配置

### 查看当前配置
```
串口命令：config

输出示例：
============= CONFIGURATION =============
Open angle: 0°
Close angle: 270°
Movement speed: 5°/100ms
Current threshold: 300 mA
Vibration time: 200 ms
=========================================
```

### 保存配置
所有改动使用 `set` 命令后会自动保存到 EEPROM：
```
set open 10             → 立即保存
set close 260           → 立即保存
set speed 7             → 立即保存
set threshold 350       → 立即保存
set vibrate 150         → 立即保存
```

### 恢复默认配置
```
set open 0
set close 270
set speed 5
set threshold 300
set vibrate 200
```

### 导出当前配置
```
1. 执行：config
2. 手动记录或截图输出
3. 保存到文本文件备份
```

---

## 故障排除

### 问题：修改参数后没有保存
**解决：** 使用 `config` 命令验证，如果仍未改变，尝试：
```
set open 0              （重新设置参数）
等待1秒
config                  （验证是否已保存）
```

### 问题：EEPROM 出现错误
**解决：** 执行完整重置
```
1. 执行：set open 0
2. 执行：set close 270
3. 执行：set speed 5
4. 执行：set threshold 300
5. 执行：set vibrate 200
6. 重启 Arduino
```

### 问题：找不到合适的参数组合
**解决：** 
```
1. 从默认值开始
2. 每次只改动一个参数
3. 测试效果后再改下一个
4. 使用模板作为起点
```

---

## 总结

- **打开/关闭角度：** 定义运动范围
- **运动速度：** 平衡效率和精度
- **电流阈值：** 调节物体检测灵敏度
- **振动时长：** 提供用户反馈强度

合理配置这 5 个参数，可以让电动夹爪系统适应各种不同的应用场景。
